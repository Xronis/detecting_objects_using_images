language: python

python:
  - "3.5"

install:
  - pip install -r requirements.txt
  - pip install .

script:
  - pytest
  - py.test run ./testing/test_image_loader.py --cov=sample.py

after_success:
  - codecov